{
  "name": "ski-game",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 scripts/serve.py",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "three": "^0.170.0"
  }
}